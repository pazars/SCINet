from setuptools import find_packages
from setuptools import setup

setup(
    name="scinet-train-sample",
    version="1.0",
    packages=find_packages(),
    include_package_data=True,
    description="SCINet sample training application.",
)
